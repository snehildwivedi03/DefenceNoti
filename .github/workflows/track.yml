name: Defence Notification Tracker

on:
  schedule:
    # Twice daily (UTC). 04:30 UTC = 10:00 IST, 16:30 UTC = 22:00 IST (10:00 PM).
    - cron: '30 4 * * *'
    - cron: '30 16 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: tracker
  cancel-in-progress: false

jobs:
  track:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Run tracker
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
        run: npm run track

      - name: Commit pruned state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/notifications.json
          if git diff --staged --quiet; then
            echo "No changes."
          else
            git commit -m "chore: update notifications state [skip ci]"
            git push
          fi
